<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student</title>
</head>

<body>
    <div class="container flex-center">
        <div class="header">
            <h1>Edit Student</h1>
        </div>
        <div class="err">
            <% if(typeof errors!=='undefined' ){ %>
                <p>
                    <%= errors.validationError %>
                </p>
                <% } %>
        </div>
            <form class="flex-center" action="/student/edit/<%= data?._id %>" method="post">
                <input type="hidden" name="_id" value="<%= data?._id %>">
                    <input type="text" placeholder="enter your name" name="name" value="<%= data?.name %>" required>
                    <br/><br/>
                    <input type="password" name="password" placeholder="enter password" value="<%= data?.password %>"
                        id="" required>
                    <br/><br/>
                        <input type="email" name="email" id="" placeholder="enter your email" value="<%= data?.email %>"
                        required>
                        <br/><br/>
                    <input type="text" name="city" id="" placeholder="enter your city" value="<%= data?.city %>"
                        required>
                        <br/><br/>
                    <input type="number" name="age" id="" min="10" max="80" placeholder="Age here"
                        value="<%= data?.age %>" required>
                        <br/><br/>
                    <input type="radio" name="gender" value="Male" id="" <% if(data?.gender==='Male' ) {%>checked<%} %>
                        >Male
                        <input type="radio" name="gender" value="Female" id="" <% if(data?.gender==='Female' ){%>
                        checked<% }%>>Female
                        <br/><br/>
                    <input type="submit" value="Update">
            </form>
        </div>
    </div>
    <div class="links mt-2">
        <a href="/logout">Log Out</a>
    </div>
</body>

</html>